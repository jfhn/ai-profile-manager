// STUB — implemented by the t3 module.
import type { FastifyInstance } from 'fastify';
import type { AppContext } from '../context.js';

export function registerT3Routes(_app: FastifyInstance, _ctx: AppContext): void {
  // t3 instance CRUD + start/stop
}

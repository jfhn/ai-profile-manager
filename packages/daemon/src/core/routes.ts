// STUB — implemented by the daemon-core module.
import type { FastifyInstance } from 'fastify';
import type { AppContext } from '../context.js';

export function registerCoreRoutes(_app: FastifyInstance, _ctx: AppContext): void {
  // profiles CRUD, discovery, wizard, usage endpoints
}
